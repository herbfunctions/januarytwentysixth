on: push
jobs:
  first-job:
    runs-on: windows-latest
    steps:
        - run: echo "Hello, world!"
        - run: echo "This is my first action"
        - run: node --version
        - run: npm --version